<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <!-- Bootstrap CSS -->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
   <title>ADMIN USER PAGE</title>
</head>
<body>
   <h1>Hello ADMIN</h1>
   <div>
      <table class="table table-striped">
         <thead>
           <tr>
             <th scope="col">#</th>
             <th scope="col">username</th>
             <th scope="col">email</th>
             <th>ACTIONS</th>
             <th></th>
           </tr>
         </thead>
         <tbody>        
            <% users.forEach((user,index)=> { %>   
           <tr>           
               <td><%= index+1%></td>
               <td><%= user.username%></td>
               <td><%= user.email %></td>  
               <td><button onclick="onUpdate(index)"><i class="bi bi-pencil-fill"></i>Edit</button></td>   
               <td><button onclick="onDelete(index)"><i class="bi bi-trash3-fill"></i>Delete</button></td>      
           </tr>
           <%  }) %>
         </tbody>
       </table>
   </div>
</body>
</html>